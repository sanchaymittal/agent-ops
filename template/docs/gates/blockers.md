# {{PROJECT_NAME}} — External Blockers (detail)

Read when: a gate row in [`index.md`](./index.md) points here, or the owner is executing unblock actions.

*Inputs that agents cannot produce or invent. Live status lives in [`index.md`](./index.md) — this file holds owner actions and per-gate detail. Order sections by external latency — longest poles first.*

## Owner actions — do in this order

1. _TODO(project): ordered list of owner-side unblock actions, each linking its runbook and gate row._

## 1. _Gate name — what it blocks_

- **What's needed:** _exact artifact/credential/decision._
- **Who:** _owner name / external party, expected latency._
- **Blocks:** _the tasks/issues that must not start._
- **Runbook:** _link into `docs/runbooks/` if a procedure exists._

## What agents CAN do now (not blocked)

- _TODO(project): keep this list honest — scaffolds, templates, offline validators, docs._
